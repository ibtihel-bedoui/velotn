{% extends 'base.html.twig' %}
{% block front %}
<section class="latest-products spad">
        <div class="container">
            <div class="product-filter">
                <div class="row">
                    <div class="col-lg-12 text-center">
                        <div class="section-title">
                            <h2>Les  Evenements</h2>
                        </div>
                        
                    </div>
                </div>
            </div>

            <div class="row" id="product-list">
                {% for evenement in evenements %}
                <div class="col-lg-3 col-sm-6 mix all dresses bags" >
                    <div class="single-product-item" >
                        <figure  style="height: 200px">
                            <a href="{{path('evenementDetailsFront',{'id':evenement.id})}}"><img src="{{ asset('images/') }}{{ evenement.nomImage }}" alt=""></a>
                            <div class="p-status">new</div>
                        </figure>
                        <div class="product-text"  >
                           <h6 style="height: 30px"> {{evenement.libelle}}<br>
                            {{evenement.description}}<p></h6>
                            <p>de {{evenement.datedebut|date}} jusqu'à {{evenement.datefin|date}}</p>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>

            </div>
        </div>
    </section>

    {% endblock %}