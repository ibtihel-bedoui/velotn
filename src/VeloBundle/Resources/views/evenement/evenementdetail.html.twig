{% extends 'base.html.twig' %}
{% block container %}


{% set urlc= app.request.uri %}


{% set long = urlc|length %}

{% set res=  urlc|slice(56, long) %}

{% if evenement.id == res %}
<center>

<img src="{{ asset('images/') }}{{ evenement.nomImage }}"style="width: 1400px;height: 600px">

<h1>{{ evenement.libelle }}</h1>
{% endif %}
<br><br><br><br><br>

                       
                        


<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="example-wrapper">

                        {{form(form)}}

                        </div>

</center>
<br><br><br><br><br>

 <div class="col-md-12">
                        <div class="card">
                            <div class="card-header">
                                <strong class="card-title">Data Table</strong>
                            </div>
                            <div class="card-body">
                                <table id="bootstrap-data-table" class="table table-striped table-bordered">
                                    <thead>
                                        <tr>
                                            
                                            <th>Nom</th>
                                            <th>Prenom</th>
                                            <th>Numero du Dossard</th>
                                            <th>supprimer</th>
                                        </tr>
                                    </thead>
                                    <tbody>
{% for participant in participants %}
                        {% if participant.evenement.id == res %}    <tr>

        <td>{{participant.nom}}</td>
        <td>{{participant.prenom}}</td>
        <td>{{participant.numdossard}}</td>
        <td><a href="{{path('participant_supprimer',{'id':participant.id})}}">supprimer</a> </td>

        


    </tr>

{% endif %}
{% endfor %}
      
                                       
                                      
                                       
                                        
                                        
                                       
                                        
                                        
                                       
                                    
                                       
                                       
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>


{% endblock %}